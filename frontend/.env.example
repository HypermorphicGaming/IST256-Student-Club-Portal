# Frontend API base URL.
# Local development: keep as /api and rely on Vite proxy.
# Production: set this to your deployed backend URL, e.g. https://api.example.com/api
VITE_API_BASE_URL=/api

# Optional local override for Vite dev proxy target.
VITE_PROXY_TARGET=http://localhost:3000
